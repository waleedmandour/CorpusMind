"""Semantic analysis package (v1.2.0) — Vector KWIC (Anthony 2025).

Import the submodule directly:
    from semantic.vector_kwic import vector_kwic
(No `from semantic.vector_kwic import vector_kwic` re-export here: it would
shadow the submodule attribute on the package.)
"""
